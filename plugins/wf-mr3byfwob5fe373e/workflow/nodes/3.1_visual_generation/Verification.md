## 验收项

- `outputs/images/` 中存在 4-9 张图片，文件名从 `01` 开始连续编号，无跳号、无重复编号。
- `outputs/visual-summary.md` 记录已选模板名、图片总数、每张图片主题和对应文件路径。
- 所有图片使用同一模板风格，且每张图的信息重点不同。
- 图片内容与 `outputs/image-content-brief.md` 的页面规划一致。
- 图片文字短且可读，没有明显超出画面安全区域。